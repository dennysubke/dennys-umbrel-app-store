version: '3.7'

services:
  app_proxy:
    environment:
      APP_HOST: denny-vidbee_web_1
      APP_PORT: 3000
      PROXY_AUTH_ADD: "false"

  web:
    image: ghcr.io/nexmoe/vidbee-web:sha-8c7bb64@sha256:4a47950118baea92507524b161704b3d9d3349645586c4ddb1cd70a021d7963c
    restart: on-failure
    environment:
      VITE_API_URL: ""
      VIDBEE_ALLOWED_HOSTS: "${DEVICE_DOMAIN_NAME},denny-vidbee_web_1"
      __VITE_ADDITIONAL_SERVER_ALLOWED_HOSTS: "${DEVICE_DOMAIN_NAME},denny-vidbee_web_1"
    healthcheck:
      test:
        [
          "CMD-SHELL",
          "wget -qO- http://127.0.0.1:3000/ > /dev/null || exit 1"
        ]
      interval: 10s
      timeout: 5s
      retries: 10
      start_period: 20s

  api:
    image: ghcr.io/nexmoe/vidbee-api:sha-8c7bb64@sha256:6e8f5ac3070aff35319164535ff3c5a404631c7a0495ec3093c72edf56d9084e
    network_mode: "service:web"
    depends_on:
      web:
        condition: service_healthy
    restart: on-failure
    environment:
      VIDBEE_API_HOST: 0.0.0.0
      VIDBEE_API_PORT: 3100
      VIDBEE_DOWNLOAD_DIR: /data/downloads
      VIDBEE_HISTORY_STORE_PATH: /data/vidbee/vidbee.db
      VIDBEE_PERSIST_QUEUE: 1
    volumes:
      - ${UMBREL_ROOT}/data/storage/downloads/vidbee:/data/downloads
      - ${APP_DATA_DIR}/data:/data/vidbee
    healthcheck:
      test:
        [
          "CMD-SHELL",
          "wget -qO- http://127.0.0.1:3100/health > /dev/null || exit 1"
        ]
      interval: 10s
      timeout: 5s
      retries: 10
      start_period: 30s
