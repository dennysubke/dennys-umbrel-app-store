version: "3.7"

services:
  web:
    image: nocturno/hyper-hdr:21.0.0.0
    restart: on-failure

    environment:
      TZ: Europe/Berlin
      PUID: "1000"
      PGID: "1000"

    volumes:
      - ${APP_DATA_DIR}/data/config:/config

    ports:
      - "8098:8090"
